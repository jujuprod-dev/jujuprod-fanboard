export type ProjectData = {
    title: string;
    description: string;
    imageURL: string;
    notionLink: string;
    status: string;
    points: number;
    progress: number;
    projectId: string;
}